import Constants from "expo-constants";

export const API_URL = Constants.expoConfig?.extra?.API_URL;
export const API_KEY = Constants.expoConfig?.extra?.SECRET_KEY;
export const TOKEN_KEY = Constants.expoConfig?.extra?.TOKEN_KEY;